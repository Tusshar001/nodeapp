version: 0.2

phases:
  install:
    commands:
      - echo Installing dependencies

  build:
    commands:
      - echo Build started on `date`
      - echo Preparing artifact for Elastic Beanstalk

artifacts:
  files:
    - '**/*'